export enum ErrorMessage {
  NoError = '',
  LoadingPosts = 'Loading posts failed',
  LoadingComments = 'Loading comments failed',
  DeletingComment = 'Comment deleting failed, try again',
  PostingComment = 'Comment posting failed',
}
